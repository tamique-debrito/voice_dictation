# Audio recording
pyaudio==0.2.14

# Keyboard shortcuts + keystroke simulation (paste, backspace cleanup)
pynput==1.7.6

# Clipboard management
pyperclip==1.9.0

# HTTP requests (for transcription APIs)
requests==2.32.3

# Environment variable management
python-dotenv==1.0.1

# UI feedback (terminal output)
rich==13.9.4

# Local transcription provider (optional; requires ffmpeg on PATH)
openai-whisper==20250625

# Persistent streaming mode
faster-whisper>=1.2.1
webrtcvad==2.0.10
numpy>=1.26
websockets>=12.0
